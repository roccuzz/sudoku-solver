# Sudoku Solver

Solves a 9x9 Sudoku puzzle from a text file using recursive backtracking.

## Compile

```bash
make
```

## Run

```bash
./sudoku_solver <file>
```

The input file must contain exactly 81 digits (0 = empty cell). All other characters are ignored.
